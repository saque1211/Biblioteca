/**
 * Código de afiliado embutido no app. Todos os botões "Ver na Amazon" usam
 * este código, então o dono ganha comissão nas compras de qualquer pessoa que
 * usa o app (não só no próprio aparelho). Tags de afiliado são públicas por
 * natureza — aparecem em todo link de afiliado — então podem ficar no código.
 */
export const AMAZON_TAG = 'isaquebibliot-20'
